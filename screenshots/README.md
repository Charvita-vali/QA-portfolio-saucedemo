# Screenshots

Add your evidence screenshots here, named to match the references in the bug reports:

- `bug001_dog_images.png` — problem_user Products page, all items showing dog image
- `bug001_standard_user_correct.png` — standard_user Products page, correct distinct images (comparison)
- `bug002_checkout_blocked.png` — Last Name field unresponsive during checkout
- `bug002_validation_error.png` — Validation error after attempting Continue with no Last Name

Once added, they'll render automatically in GitHub when viewing the bug report markdown files (they're already linked via relative paths).
